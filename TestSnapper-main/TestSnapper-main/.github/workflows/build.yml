name: Build TestSnapper

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install PyQt6 Pillow keyboard pynput pyinstaller
      
      - name: Build .exe
        run: |
          cd app
          copy icon.ico icon_build.ico
          pyinstaller --name TestSnapper --onefile --windowed --icon=icon_build.ico --hidden-import=pynput.keyboard._win32 --hidden-import=pynput.mouse._win32 --add-data "icon.ico;." --clean snapshotai.py
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: TestSnapper-Windows
          path: app/dist/TestSnapper.exe

  build-mac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install PyQt6 Pillow keyboard pynput pyinstaller
      
      - name: Build .app
        run: |
          cd app
          pyinstaller --name TestSnapper --onefile --windowed --osx-bundle-identifier com.testsnapper.app --hidden-import=pynput.keyboard._darwin --hidden-import=pynput.mouse._darwin --clean snapshotai.py
      
      - name: Create DMG
        run: |
          cd app/dist
          mkdir -p dmg_contents
          cp -r TestSnapper dmg_contents/ 2>/dev/null || cp TestSnapper dmg_contents/
          hdiutil create -volname "TestSnapper" -srcfolder dmg_contents -ov -format UDZO TestSnapper.dmg
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: TestSnapper-Mac
          path: app/dist/TestSnapper.dmg

  release:
    needs: [build-windows, build-mac]
    runs-on: ubuntu-latest
    steps:
      - name: Download Windows build
        uses: actions/download-artifact@v4
        with:
          name: TestSnapper-Windows
          path: ./release
      
      - name: Download Mac build
        uses: actions/download-artifact@v4
        with:
          name: TestSnapper-Mac
          path: ./release
      
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release/TestSnapper.exe
            release/TestSnapper.dmg
          generate_release_notes: true
